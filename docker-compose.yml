services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: pleasanton
      POSTGRES_PASSWORD: pleasanton
      POSTGRES_DB: pleasanton
    ports:
      - "5432:5432"
    volumes:
      - pleasanton_pgdata:/var/lib/postgresql/data

volumes:
  pleasanton_pgdata:

